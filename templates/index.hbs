<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{blog_title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.1/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50 text-gray-900">

<header class="bg-white shadow">
    <div class="container mx-auto px-6 py-4">
        <div class="flex items-center justify-between">
            <div>
                <a href="#" class="text-xl font-bold text-gray-800 md:text-2xl">{{blog_name}}</a>
            </div>
            <div>
                {{#each navigation}}
                    <a href="{{url}}" class="text-gray-800 text-sm font-semibold hover:text-gray-600">{{name}}</a>
                {{/each}}
            </div>
        </div>
    </div>
</header>

<main class="container mx-auto px-6 pt-16">
    <div class="flex flex-wrap -mx-4">
        <!-- Main content -->
        <div class="w-full lg:w-2/3 px-4">
            <div class="mb-12">
                <h2 class="text-3xl font-bold mb-6">Latest Articles</h2>
                <!-- Article card -->
                {{#each articles}}
                    <div class="bg-white rounded-lg overflow-hidden shadow-lg mb-6">
                        <img class="w-full h-64 object-cover" src="{{image_url}}" alt="Article">
                        <div class="p-6">
                            <div class="font-bold text-xl mb-2">{{title}}</div>
                            <p class="text-gray-700 text-base">
                                {{summary}}
                            </p>
                            <div class="flex items-center justify-between mt-4">
                                <a href="{{read_more_url}}" class="text-blue-600 hover:underline">Read more</a>
                                <span class="text-sm font-light">{{published_date}}</span>
                            </div>
                        </div>
                    </div>
                {{/each}}
                <!-- More articles -->
            </div>
        </div>
        <!-- Sidebar -->
        <div class="w-full lg:w-1/3 px-4">
            <div class="mb-12">
                <h3 class="text-2xl font-bold mb-4">About Me</h3>
                <div class="bg-white rounded-lg p-6 shadow-lg">
                    <p class="text-gray-700 mb-4">
                        {{about_me}}
                    </p>
                    <a href="{{about_me_url}}" class="text-blue-600 hover:underline">Learn more about me</a>
                </div>
            </div>
            <!-- More sidebar content -->
        </div>
    </div>
</main>

<footer class="bg-white shadow mt-16">
    <div class="container mx-auto px-6 py-4">
        <div class="flex justify-between items-center">
            <p class="text-gray-700 text-sm">© {{current_year}} {{blog_name}}. All rights reserved.</p>
            <div>
                <a href="{{privacy_policy_url}}" class="text-gray-700 text-sm font-semibold hover:text-gray-600">Privacy Policy</a>
                <a href="{{terms_conditions_url}}" class="ml-4 text-gray-700 text-sm font-semibold hover:text-gray-600">Terms & Conditions</a>
            </div>
        </div>
    </div>
</footer>

</body>
</html>